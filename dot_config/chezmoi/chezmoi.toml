[edit]
    command = "code"
    args = ["--wait", "--new-window"]
[merge]
    command = "code"
    args = ["--wait", "--new-window", "--diff", "{{ .Source }}", "{{ .Destination }}"]
    #args = [".Source:{{ .Source }}", ".Destination:{{ .Destination }}", ".Target:{{ .Target }}"]

[keepassxc]
    database = "/home/horo/keepass/horo.kdbx"
    args = [ "-k", "/home/horo/.horo/horo.key"]

